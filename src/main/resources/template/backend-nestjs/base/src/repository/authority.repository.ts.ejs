import { Authority<%-entitySuffix%> } from "../domain/authority.entity";
import { AbstractBaseRepository } from "./abstract.base.repository";
import { Injectable } from "@nestjs/common";
import { InjectRepository } from "@nestjs/typeorm";

@Injectable()
export class AuthorityRepository extends AbstractBaseRepository<Authority<%-entitySuffix%>> {
  constructor(@InjectRepository(Authority<%-entitySuffix%>) repository: AbstractBaseRepository<Authority<%-entitySuffix%>>) {
    super(repository);
  }
}
