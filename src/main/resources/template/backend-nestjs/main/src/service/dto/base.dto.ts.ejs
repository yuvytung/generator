<%-database==="mongodb" ? `import { Transform } from 'class-transformer';` : ''%>
/**
 * A <%-dtoSuffix%> base object.
 */
export class Base<%-dtoSuffix%> {
  <%-database==="mongodb"
  ?`_id?: string;

  @Transform((id) => (id?.value.toHexString ? id?.value.toHexString() : id), { toPlainOnly: true })
  id?: string;`
  : `id?: number;`%>

  createdBy?: string;

  createdDate?: Date;

  lastModifiedBy?: string;

  lastModifiedDate?: Date;
}
