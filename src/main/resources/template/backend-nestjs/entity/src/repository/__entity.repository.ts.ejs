<% const entityName=_entity.name.pascal().concat(entitySuffix) -%>
import { <%-entityName%> } from '../domain/<%-_entity.name.kebab()%>.entity';
import { AbstractBaseRepository } from './abstract.base.repository';
import { Injectable } from "@nestjs/common";
import { InjectRepository } from "@nestjs/typeorm";

@Injectable()
export abstract class <%-_entity.name.pascal()%>Repository extends AbstractBaseRepository<<%-entityName%>> {
  protected constructor(@InjectRepository(<%-entityName%>) repository: AbstractBaseRepository<<%-entityName%>>) {
    super(repository);
  }
}
