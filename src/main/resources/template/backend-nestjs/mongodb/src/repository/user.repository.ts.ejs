import { AbstractBaseRepository } from "./abstract.base.repository";
import { Injectable } from "@nestjs/common";
import { InjectModel } from "@nestjs/mongoose";
import { Model } from "mongoose";
import { User } from "../domain/user.entity";
import { MongooseCollection } from "../domain/base/mongoose.collection";

@Injectable()
export class UserRepository extends AbstractBaseRepository<User> {
  constructor(@InjectModel(MongooseCollection.User) protected _model: Model<User>) {
    super(_model);
  }
}
