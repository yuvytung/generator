import { Authority } from "../domain/authority.entity";
import { AbstractBaseRepository } from "./abstract.base.repository";
import { Injectable } from "@nestjs/common";
import { InjectModel } from "@nestjs/mongoose";
import { Model } from "mongoose";

@Injectable()
export class AuthorityRepository extends AbstractBaseRepository<Authority> {
  constructor(@InjectModel(Authority.Name) protected _model: Model<Authority>) {
    super(_model);
  }
}
