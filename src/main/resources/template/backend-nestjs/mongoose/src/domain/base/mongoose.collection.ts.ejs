<%
const entitiesName = _entities.map((e)=> e.name )
-%>
// eslint-disable-next-line no-shadow
export enum MongooseCollection {
  User = "sys_user",
  Authority = "sys_authority",
  <%- entitiesName.map((name)=> `${name.pascal()} = "${name.snake()}"`).join(`,
  `)%>
};
