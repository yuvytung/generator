<%
const entityName = _entity.name.pascal().concat(entitySuffix);
-%>
import { <%-entityName%> } from '../domain/<%-_entity.name.kebab()%>.entity';
import { AbstractBaseRepository } from './abstract.base.repository';
import { Injectable } from "@nestjs/common";
import { InjectModel } from "@nestjs/mongoose";
import { Model } from "mongoose";
import { MongooseCollection } from "../domain/base/mongoose.collection";

@Injectable()
export class <%-_entity.name.pascal()%>Repository extends AbstractBaseRepository<<%-entityName%>> {
  constructor(@InjectModel(MongooseCollection.<%-entityName%>) repository: Model<<%-entityName%>>) {
    super(repository);
  }
}
