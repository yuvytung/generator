import { Authority } from "../../domain";
import { BaseRepository } from "../../common/bases";
import { Injectable } from "@nestjs/common";
import { InjectModel } from "@nestjs/mongoose";
import { Model } from "mongoose";
import { EntitiesEnum } from "../../common/enums/EntitiesEnum";
import { AuthorityRepository } from "../";

@Injectable()
export class AuthorityRepositoryImpl
  extends BaseRepository<Authority>
  implements AuthorityRepository
{
  protected readonly esIndex: string;
  constructor(@InjectModel(EntitiesEnum.Authority) protected model: Model<Authority>) {
    super();
  }
}

export default { provide: AuthorityRepository, useClass: AuthorityRepositoryImpl };
